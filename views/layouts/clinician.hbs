{{! The code for the function 'check_password()' is very similar to the solutions presented on this page
https://stackoverflow.com/questions/21727317/how-to-check-confirm-password-field-in-form-without-reloading-page,
with modifications to variable names and output messages. }}
<html lang="en">
  <head>
    <!-- Start of copied code (with some modifications). This function ensures that 'password' and
    'confirm-password' have the same value. If they do, the user is given an affirmative message
    and the submit button is enabled. Otherwise, the user is informed the passwords don't match
    and the submit button is disabled.-->
    <script>
    function check_password() {
      if (document.getElementById("password").value == document.getElementById("confirm-password").value) {
        document.getElementById("submit").disabled = false;
        document.getElementById("feedback").innerHTML = "Passwords match :)";
        document.getElementById("feedback").style.color = "green";
      } else {
        document.getElementById("submit").disabled = true;
        document.getElementById("feedback").innerHTML = "Passwords do not match";
        document.getElementById("feedback").style.color = "red";
      }
    }
    </script>
    <!-- End of copied code -->
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/{{style}}" />
    <link rel="stylesheet" href="/css/allClinician.css" />
    <title>Diabetes@Home</title>
  </head>
  <body>
    <header class="layout-header">
      <h1 class="header-title">D@H</h1>
      <div class="header-doctor">
        Dr.
        {{clinician.firstName}}
        {{clinician.lastName}}</div>
    </header>
    {{! main content}}
    <div class="clin-navbar"> 
      {{> myNav}}
      {{{body}}}
    </div>
    <footer class="layout-footer">
      <p>© Diabetes@Home 2022 &nbsp;&nbsp;|&nbsp;&nbsp; P: 0123456789 &nbsp;&nbsp;|&nbsp;&nbsp; E: <a href="mailto:diabetes.home@gmail.com">diabetes.home@gmail.com</p>
    </footer>
  </body>
</html>